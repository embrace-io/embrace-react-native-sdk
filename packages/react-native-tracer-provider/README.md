# react-native-tracer-provider

A React Native wrapper over the Embrace SDK that conforms to the OpenTelemetry TracerProvider interface

## Native Testing

Android (TODO)

```
    cd native-tests/android
    ./gradlew :app:testDebugUnitTest
```
